var ehTable = {
    insert: function (table, index) {
        var row = document.createElement("div");
        row.classList.add("row");
        table.insertBefore(
            row,
        )
    }
}
